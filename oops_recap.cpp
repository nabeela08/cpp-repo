#include<iostream>
using namespace std;

int main()
{
    // OOPS - Classes and Object

    // structures has limitations i.e. members are public and we can't write memebers in it.
    // structures in c++ are typedef.

    // classes - extension of structures.
    // classes can have methods and properties.
    // classes can make few members as private and public both.

    //we can declare objects along with the class declaration:
    // class employee{

    // } stef, elena, damon;
    // stef.salary = 100; makes no sense if it is private


    cout<<endl;
    return 0;
}